import actions from './actions';

const startTimer = actions.startTimer;
const restartTimer = actions.restartTimer;
const addSecond = actions.addSecond;

export default {
    startTimer,
    restartTimer,
    addSecond
};
